# node / frontend
node_modules/
dist/
.env
.env.*
*.log
.DS_Store

# python / uv
__pycache__/
*.py[cod]
.venv/
.uv-cache/
.uv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# app data
backend/data/*.json
!backend/data/plans.json
# Android SDK (local)
.android-sdk/

# SSL certs (never commit)
nginx/ssl/*.pem
nginx/ssl/*.key
nginx/ssl/*.crt

# server-specific compose overrides (never commit, preserved by reset --hard)
docker-compose.override.yml
docker-compose.override.*.yml

# postgres 宿主机数据目录 (deploy 持久化)
pgdata/
